services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      # Persist DB across container restarts
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
